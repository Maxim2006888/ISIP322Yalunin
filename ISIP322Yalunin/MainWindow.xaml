<Window x:Class="ISIP322Yalunin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ISIP322Yalunin"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock>
               Код: 
            </TextBlock>
            <TextBox x:Name="CodeTextBox" Width="80" Margin="0,0,5,0"/>
            <TextBlock>
                Название: 
            </TextBlock>
            <TextBox x:Name="NameTextBox" Width="120" Margin="0,0,25,0"/>
            <TextBlock>
                Цена: 
            </TextBlock>
            <TextBox x:Name="PriceTextBox" Width="80" Margin="0,0,25,0"/>
            <TextBlock>
                Количество: 
            </TextBlock>
            <TextBox x:Name="QuantityTextBox" Width="80" Margin="0,0,25,0"/>
            <TextBlock>
                Категория: 
            </TextBlock>
            <TextBox x:Name="CategoryTextBox" Width="120" Margin="0,0,25,0"/>
            <Button Content="Добавить" Click="AddProductButton_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="0,0,5,0"/>
            <Button Content="Поиск" Click="SearchButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="ProductsDataGrid" Grid.Row="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Code}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="10">
            <TextBlock>
                Код для продажи:
            </TextBlock>
            <TextBox x:Name="SellCodeTextBox" Width="80" Margin="0,0,5,0"/>
            <TextBlock>
                Количество:
            </TextBlock>
            <TextBox x:Name="SellQuantityTextBox" Width="80" Margin="0,0,5,0"/>
            <Button Content="Продать" Click="SellProductButton_Click"/>
            <TextBlock>
                Код для удаления:
            </TextBlock>
            <TextBox x:Name="RemoveCodeTextBox" Width="80" Margin="0,0,5,0"/>
            <Button Content="Удалить" Click="RemoveProductButton_Click"/>
        </StackPanel>
    </Grid>

</Window>
